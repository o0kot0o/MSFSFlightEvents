# Build output from fspackagetool / Project Editor
Packages/
PackagesMetadata/
_PackageInt/

# npm
node_modules/
package-lock.json

# esbuild output
PackageSources/*/dist/

# Vendored Microsoft SDK package - not on the public npm registry, and not
# ours to redistribute. Copy it in locally from your own MSFS SDK install
# before building (see PackageSources/vendor/README.md).
PackageSources/vendor/*.tgz
